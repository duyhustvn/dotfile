---
# tasks file for bootstrap

- name: Install base system packages
  include_tasks: system_packages.yml
  tags: [bootstrap, system_packages]

- name: Install Nerd Fonts
  include_tasks: install_fonts.yml
  tags: [bootstrap, fonts]

- name: Configure fzf
  include_tasks: configure_fzf.yml
  tags: [bootstrap, fzf]
